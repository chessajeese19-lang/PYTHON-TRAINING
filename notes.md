# Headline
# Types of operators

1. Arithmatic Operator: +,-,/,//,%,**
2. Relational Operator: ==,!=,>,< etc
3. Assignment Operator: =,+=,-=,*=,//=,/= etc
4. Logical Operator: and,or,not
5. Bitwise operator: &,~,<<,>>,^
6. Membership Operator: in, not in
7. Identity Operator:is
8. Ternary Operator

#  Types of collections in python

# 1. list:Heterogeneous []--> [1,2,3] 
    num=[]
    num=list()
    (any type of element it can store)
    (it can store duplicates)
    (its mutable)
# 2. Tuple: immutable data structure
     () or tuple()
     not changeable
     can store duplicates
     heterogeneous
     faster than list

# 3. Dictionary: key value pairs
    {} Dictionary()
    {Key:Value}
    empty dictionary--> num={} mutable
# 4. Sets: Stores only unique elements
     {} Set()
     empty set-->Set()

num=[()]--> list of tuples
num=[[]]--> list of lists  or [[],[]]
(())-->tuple of tuple
{[]:[]}-->dictionary of lists
[(),{},set()]--> list

Arguments:
An argument is also called parameter
its the value passed to function
value that you give to the parameter is called an argument

#types of arguments:
1.positional
2.default
3.arbitrary
4.arbitrary keyword
5.mixed

#argument packing
Arguments passed to the parameter and then it is packed as tuple, list and dictionary

#argument unpacking
dictionary to the main
#lambda function
small anonymous function in python
1.where its used?
-map(),filter(),reduce(),sorted()
2.types of lambda functions
lambda with no parameter
lambda with one parameter
lambda with multiple parameter
lambda with coonditional expression
nested lambda

#classes and objects in python
1.class
blueprint of object
it contains methods and data
a datatype used to create instance

2.object
an instance of class

#encapsulation
binding of data and functions
example: getters setters and class 

#constructors
is a special method that is automatically called when object is created






